---
name: verify
description: Type-check and build the project to verify changes are valid. Use after making code changes.
---

Run the following commands to verify the project:

1. Type-check: `npx tsc --noEmit`
2. Build: `npx vite build`

Report any errors found. If both pass, confirm the project builds cleanly.
